version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign
    - copyloopvar
    - misspell
    - bodyclose
    - noctx
  settings:
    errcheck:
      check-type-assertions: true
      exclude-functions:
        - (io.Closer).Close
        - (*net.Conn).Close
        - os.Remove
        # Diagnostic writes to an io.Writer param (CLI stdout/stderr): errcheck
        # already ignores fmt.Fprint* to os.Stdout/os.Stderr literals, but the
        # CLI commands take writers for testability, so exclude them explicitly.
        - fmt.Fprint
        - fmt.Fprintf
        - fmt.Fprintln
    govet:
      enable-all: true
      disable:
        - fieldalignment
  exclusions:
    rules:
      # Eclipse Mosquitto — and its bundled mosquitto_pub/mosquitto_sub CLI
      # tools — are the correct product/binary spelling (not "Mosquito").
      # Case-insensitive so it also covers the all-lowercase CLI binary
      # names used in interop tests and CI (mosquitto_pub, mosquitto_sub).
      - linters: [misspell]
        text: "(?i)mosquitto"
