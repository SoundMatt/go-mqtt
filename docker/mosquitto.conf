# Minimal Mosquitto configuration for the go-mqtt quickstart.
# Allows anonymous connections on the default MQTT port and a WebSocket port.
allow_anonymous true

# Plain MQTT over TCP.
listener 1883

# MQTT over WebSocket (RFC 6455) for the v3 DialWS client and browsers.
listener 9001
protocol websockets
